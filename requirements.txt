streamlit>=1.32.0

git+https://github.com/HKUDS/LightRAG.git@main

# LLM clients
openai>=1.30.0
groq>=0.9.0

# Embeddings (HuggingFace — no Ollama dependency)
sentence-transformers>=3.0.0
numpy>=2.0.0
einops

# Vector store
qdrant-client==1.13.3
# downgrade - Lightrag use qdrant .search() method, in 1.17+ -> qdrant use .query_points

# Graph store
neo4j>=5.19.0

# Tokenization
tiktoken>=0.7.0

# Environment
python-dotenv>=1.0.0

# PDF extraction
pymupdf>=1.24.0

# Async / HTTP
aiohttp>=3.9.0
nest-asyncio>=1.6.0
requests>=2.31.0

# Evaluation (eval/eval_script.py)
tqdm>=4.66.0
tabulate>=0.9.0

# MinerU PDF extraction (recommended, optional — falls back to PyMuPDF if not installed)
# pip install "magic-pdf[full]"